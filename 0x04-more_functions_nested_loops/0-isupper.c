#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: charachter to be checked
 * Return: 1 if c uppercase or 0 otherwhise
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
